version: "3"
services:
  mongo:
    container_name: mongo-doenet
    image: mongo
    restart: always
    ports:
      - "27017"
    networks:
      - default
    volumes:
      - ./server/docker-setup/mongo/db-init.js:/docker-entrypoint-initdb.d/init-mongo.js:ro
      - ./server/docker-setup/mongo/mongo-volume:/data/db
    environment:
      - MONGO_INITDB_DATABASE=mern_docker_starter
      - MONGO_INITDB_USER=local_user
      - MONGO_INITDB_PASSWORD=Password123

  backend:
    container_name: node-doenet
    build:
      context: ./server
      dockerfile: docker-setup/node/production/Dockerfile
    networks:
      - default
      - frontend
    environment:
      - NODE_ENV=production
    env_file:
      - ./server/.env.production
    tty: true
    stdin_open: true
    depends_on:
      - mongo
    ports:
      - "4000"

  frontend:
    container_name: react-doenet
    build:
      context: ./frontend
      dockerfile: docker-setup/react/production/Dockerfile
    networks:
      - frontend
    environment:
      - NODE_ENV=production
    depends_on:
      - backend
    tty: true
    stdin_open: true
    ports:
      - "80:80"
      - "443:443"

networks:
  default:
    name: backend-network-doenet
  frontend:
    name: frontend-network-doenet
